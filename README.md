# dsa-playground

เขียนเล่นตามหนังสือ

**Setup**
```bash
cmake --preset default
```

**Build**
```bash
./buid <filename> [--run|-r] [args...]
./build EuclidsAlgo
./build EuclidsAlgo -r 10 15
```

**Run**
```bash
./run <filename> [args...]
./run HelloWorld "Thas"
```
