# dsa-playground

เขียนเล่นตามหนังสือ

**Setup**
```bash
cmake --preset default
```

**Build**
```bash
./build <filename> 
./build [--all|-a]
./build EuclidsAlgo
```

**Run**
```bash
./run <filename> [args...]
./run HelloWorld "Thas"
```
